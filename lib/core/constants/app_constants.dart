class AppConstants {
  AppConstants._();

  static const String appName = 'Flutter Weather';
  static const int forecastDays = 5;
  static const int hourlyForecastCount = 24;
  static const Duration cacheExpiry = Duration(minutes: 30);
}
